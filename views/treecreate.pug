extends layout
block content
  h1= title
  form(id='TreeForm', action="/resource/trees", method="POST")
    div.form-group
      label.treeAttr(for='tree_species')  tree species
      input#tree_species(type='text', name='tree_species')
    div.form-group
      label.treeAttr(for='tree_age') Tree age
      input#tree_age(type='number', name='tree_age')
    div.form-group
      label.treeAttr(for='tree_height') Tree height
      input#tree_height(type='text', name='tree_height')
    input(type="submit")
  script.
    document.forms['TreeForm'].addEventListener('submit', (event) => {
    event.preventDefault();
    // TODO do something here to show user that form is being submitted
    fetch(event.target.action, {
    method: 'POST',
    body: new URLSearchParams(new FormData(event.target)) // event.target is the form
    }).then((resp) => {
    return resp.json(); // or resp.text() or whatever the server sends
    }).then((body) => {
    alert("Save succeded")
    // TODO handle body
    }).catch((error) => {
    // TODO handle error
    alert("Save failed " + error )
    });
    });